"""
1. Definition -
Code with functionality, written and stored in packages which is reusable
Each module has a specific purpose of usage

2. Built-in modules
Pre-installed in python Runtime environment (cpython, PyPy)
Both cpython and PyPy use python language, but it behaves differently in each



3. External modules
Package manager - pip install, pip uninstall, pip freeze - all packages installed
pip freeze > requirements.txt
pip install -r requirements.txt

4. Custom modules
Any .py file can be imported and used as a module

Creating custom package - + __init__
from triangle import print_triangle - Function from folder (__init__.py)
from triangle.line import print_line - Function from folder (line.py)
"""